import { Routes } from '@angular/router';
import { TransactionsDashboard } from './transactions/transactions-dashboard';

export const routes: Routes = [{ path: '', component: TransactionsDashboard }];
